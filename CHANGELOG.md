# Changelog

All notable changes to **Manifest Tulpa** are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] — 2026-06-22

> First **stable** release. After twelve-plus smoke cycles the automation is solid — cast dialog, modification picker, summon wiring, in-combat behaviors, and lifecycle management all working — so the module graduates from `0.1.x` to `1.0.0`. The only functional change in this release is cosmetic: the Tulpa actor finally has a face. Its sheet portrait and map token shipped the generic dnd5e `npc.svg` placeholder through v0.1.18; v1.0.0 replaces both with a commissioned portrait bundled into the module. No mechanics, cast/dismiss flow, animation, or dependency changes.

### Added

- **Commissioned Tulpa portrait on the actor sheet and map token.** A new bundled asset [`assets/tulpa.jpg`](assets/tulpa.jpg) (served at runtime as `modules/manifest-tulpa/assets/tulpa.jpg`) replaces the `systems/dnd5e/icons/svg/actors/npc.svg` placeholder on both the actor `img` (sheet portrait) and `prototypeToken.texture.src` (map token) in [`_source/manifest-tulpa-actors/Actor.tulpa.json`](_source/manifest-tulpa-actors/Actor.tulpa.json). The prototype token already used `fit: "contain"`, so the portrait is letterboxed into the 1×1 token square rather than stretched. The actor `img`, the token `src`, and the on-disk presence of the asset are locked by three new assertions in [tests/spell-source.test.mjs](tests/spell-source.test.mjs) (v1.0.0 actor-art lock).

### Internal

- **Version bumped `0.1.18` → `1.0.0`** in [module.json](module.json). The `CLAUDE.md` "Current version" line — which had drifted to v0.1.15 — is corrected to v1.0.0, and a new locked-fields row records the actor-art lock.
- **Smoke matrix extended** with R61/R62 (actor portrait on sheet + token) in [docs/superpowers/test-plans/2026-05-24-manifest-tulpa-smoke.md](docs/superpowers/test-plans/2026-05-24-manifest-tulpa-smoke.md).
- **Test count 124 → 127.** `npm test` green; `npm run validate` → "Validation passed." (the validator imposes no constraint on actor `img`/token `src`, so the swap is validator-safe); `npm run build:packs` rebuilds cleanly. No generator was created or restored — the actor JSON was hand-edited and locked in the same commit, per source-tree discipline.

## [0.1.18] — 2026-06-18

> Single-bug remediation release. A live-demo cast surfaced two linked defects in the cast dialog: (1) the modification budget was **stuck at 2 for every cast** — a 9th-level slot should allow 6 — and (2) the counter read "Slots used: …", conflating spell slots with modification slots. Root cause of (1) is a **timing bug**, not a math bug: the budget input was read at the wrong moment in dnd5e's activity lifecycle. Modules-and-tests only; no `_source/` change.

### Fixed

- **Modification budget stuck at 2 regardless of the expended spell slot (live-demo failure).** Root cause confirmed via adversarial source analysis of dnd5e 5.2.5's `ActivityUsageMixin.use()`. `cast-flow.js` captured the slot level in its `dnd5e.preUseActivity` handler by *eagerly parsing* `usageConfig` at capture time: `SLOT_CAPTURE.set(activity.uuid, parseSlotLevel(usageConfig))`. But `dnd5e.preUseActivity` ([mixin.mjs:221](.understand-anything/dnd5e-research/dnd5e/module/documents/activity/mixin.mjs)) fires **before** the slot-selection dialog (L224–230) writes the chosen slot into `usageConfig.spell.slot`, and **before** `_prepareUsageScaling` (L233) sets `usageConfig.scaling`. At capture time `spell.slot` is still the pre-dialog default (`"spell5"`) and `scaling` is undefined → `parseSlotLevel` always returned **5** → `Math.min(6, 2 + max(0, 5−5))` always **2**, no matter which slot was expended. The same always-5 `castConfig.slotLevel` also made the cast chat card read "Slot: 5" for a 9th-level cast. Fix: the slot level is now resolved **late, at `postSummon`**, after dnd5e has mutated `usageConfig` in place. `onPreUseActivity` stashes the **live `usageConfig` object** (the same `const` reference from [mixin.mjs:191](.understand-anything/dnd5e-research/dnd5e/module/documents/activity/mixin.mjs), never reassigned, so it carries the dialog's writes by the time we read it); `onPostSummon` calls the new pure `resolveSlotLevel(usageConfig)` then `modBudget(level)`. The resolution logic is byte-for-byte the prior `parseSlotLevel` (slot-key regex first, then a `typeof scaling === "number"` upcast-delta fallback, else 5) — only its *timing* moved. This stays within the existing `SLOT_CAPTURE` architecture and does not touch the `postUseActivity`/`postSummon` hook choice (invariant #2).
- **Cast-dialog counter said "Slots used", not "Modifications used" (verbiage).** The `MANIFEST_TULPA.Dialog.SlotsUsed` value in [lang/en.json](lang/en.json) now reads `"Modifications used: {used} / {max}"`, so the dialog no longer conflates the expended spell slot with the modification budget. The i18n **key** stays `SlotsUsed` (the template and dialog reference it); only the displayed value changed. The chat card's `<strong>Slot:</strong>` label is a genuine spell-slot reference and is intentionally left as-is (it now shows the correct level transitively).

### Internal

- **New Foundry-free module [modules/slot-budget.js](modules/slot-budget.js).** Exports `resolveSlotLevel(usageConfig)` and `modBudget(level)` — extracted (like `buildStrikeParts` and `dismiss-helpers.js`) so the level/budget logic is pure and Node-testable; `cast-flow.js` itself can't be imported in tests (its import chain evaluates `foundry.applications.api` at module load). The local `parseSlotLevel` helper in `cast-flow.js` was removed.
- **New [tests/slot-budget.test.mjs](tests/slot-budget.test.mjs) (16 tests).** `resolveSlotLevel` across slot keys `spell5`–`spell9`, the `pact` non-numeric fallback, the `scaling === 0` base-cast guard (the `typeof`-number trap), the no-data → 5 default, and null/undefined safety; `modBudget` for the full 5→2 … 9→6 table plus cap/floor; two end-to-end `resolve→budget` checks (9th → 6, 5th → 2). Three **source-shape locks** on `cast-flow.js` pin the timing fix: the capture stashes `usageConfig` (not an eager parse), `parseSlotLevel` is gone, and the module imports from `slot-budget.js`. One **verbiage lock** on `lang/en.json` asserts the counter says "Modifications used" and not "Slots used".
- **Test count 108 → 124.** `npm test` → **124 pass / 0 fail**. `npm run validate` → "Validation passed." `npm run build:packs` → "All packs built." (no `_source/` change; pack rebuild ran as a safety check only).
- **Known limitation (pre-existing, not regressed):** a *deferred* cast completed via the chat-card "Summon" button has no `SLOT_CAPTURE` entry at `postSummon` (the inline `preUseActivity` already cleared it), so it falls back to slot 5 / budget 2. This was already the behavior before this fix; the inline-cast path the user reported is fully corrected.

## [0.1.17] — 2026-06-06

> Post-v0.1.16-smoke remediation release, bundling all four fix passes that accumulated after v0.1.16 shipped. The v0.1.16 smoke found three RED/HIGH ship-blockers: recast destroyed both Tulpas and aborted silently (Bug #1), every duration-expiry dismissal threw a noisy red error banner (Bug #2), and the duration-expiry chat card reported the wrong dismiss reason (Bug #3). Additionally, five of twelve JB2A animation preset assets were missing in jb2a_patreon 0.8.7 and the missing-asset fast-skip guard had been dead code since v0.1.8 (Bugs #5 and #8). A fourth pass cleaned up the remaining lower-severity findings: a dead-weight DAE specialDuration key (Bug #4), an orphaned combatant left in the tracker after every dismissal (Bug #6), the system "Summon:" AE leaking when a cast is cancelled or over-budget (Bug #7), the over-budget dialog state being reachable instead of prevented (Bug #10), and the `ready` console log printing before the double-registration guard (cosmetic). Bug #9 (no animation on dismiss) and two cosmetic notes were confirmed external (Foundry core / AutoAnimations) and carry no code change. No `_source/` change in any of the four passes — this release is modules-and-tests only.

### Fixed

- **jb2a preset asset keys corrected; missing-asset fast-skip guard revived (smoke Bugs #8 and #5).** Two distinct root causes. (1) Five of twelve preset entries in [modules/animation-presets.js](modules/animation-presets.js) referenced keys that do not exist in jb2a_patreon 0.8.7: all three strike presets appended a colour suffix (`.purple` / `.yellow` / `.pinkpurple`) to `jb2a.unarmed_strike.magical`, but that family is terminal in 0.8.7 and carries no colour layer; the radiant and psychic impact presets used the `jb2a.impact.010.<colour>` numbered sub-family, which only ships `[blue, green, orange, purple, red]` — `yellow` and `pinkpurple` were never present there (an earlier v0.1.10 fix swapped one nonexistent key for another). Missing assets fell through to `.play()` and produced an unhandled `Cannot read properties of null (reading 'baseTexture')` rejection that the module's `try/catch` + `withTimeout` cannot intercept. Fixed by switching all three impacts to the base `jb2a.impact.<colour>` family (force=`purple`, radiant=`yellow`, psychic=`pinkpurple`, all verified live in jb2a_patreon 0.8.7) and dropping the colour suffix from the three strikes to the colourless `jb2a.unarmed_strike.magical`. (2) The `assetAvailable` guard in [modules/animations.js](modules/animations.js) tested `Sequencer.Database.entryExists(asset) !== false`, but Sequencer 3.6.11 returns the resolved string key when an asset is present and `undefined` when it is missing — never boolean `false`. The guard was therefore always `true`, defeating its purpose and letting every missing asset reach `.play()`. Fixed to `!= null` (missing iff null/undefined). `assetAvailable` is now exported for unit-testing. Regression locks: [tests/animation-presets.test.mjs](tests/animation-presets.test.mjs) removes the three tests that had been pinning the broken keys (a green-but-broken trap) and adds: exact base-family impact values; a universal ban on the `.impact.010.` substring across every asset string (kills the bug class); impact-key shape check (`^jb2a\.impact\.[a-z]+$`); all three strikes equal colourless `jb2a.unarmed_strike.magical`. The new [tests/animations.test.mjs](tests/animations.test.mjs) locks the Bug #5 contract against a mocked `Sequencer.Database.entryExists`: `undefined` → unavailable (the regression sentinel — this assertion flips if anyone reverts to `!== false`), `null` → unavailable, string → available, DB-absent/throws → available (timeout fallback).

- **Recast over a live Tulpa destroyed both Tulpas and silently aborted (smoke Bug #1, regression of v0.1.13 REG-2).** Root cause confirmed via live instrumentation: the recast cleanup ran in `onPostSummon` *after* dnd5e had already created the new token, and deleted the old token via a direct `performDismissCleanup` call. That old-token delete fired `onPreDeleteToken`, which deleted the old anchor *without* `skipFunnel` → the dismissal funnel re-entered. The re-entrant funnel double-deleted the system "Summon: Manifest Tulpa" AE, producing an `ActiveEffect does not exist!` banner. Crucially, with `midi-qol`'s `autoRemoveSummonedCreature = TRUE`, midi-qol pins every summoned token to a by-name-reused `"Summon: <item.name>"` AE via `flags.dnd5e.dependentOn`; both the old and new Tulpa token were pinned to the *same* reused AE, so deleting it during the old teardown cascaded — through midi-qol's token-dependent path — to the new token as well. `cast-flow.js` then attempted to delete the already-gone old anchor, threw an uncaught `undefined id … EmbeddedCollection` error, and the cast aborted before the modification dialog. Fix: a new pure helper `findPreviousAnchor(caster, moduleId)` in [modules/dismiss-helpers.js](modules/dismiss-helpers.js) locates the existing anchor by flag. The recast pre-dismissal moved from `onPostSummon` to **`dnd5e.preUseActivity`** — before dnd5e creates the new token — and now runs *through the funnel* (`anchor.delete({dismissReason:"recast", skipAnimation:true})`), not via a direct `performDismissCleanup` call. Routing through `.delete()` removes the anchor from the collection before the post-delete `deleteActiveEffect` hook runs cleanup, so the later old-token delete → `onPreDeleteToken` → `findPreviousAnchor` returns `null` → the funnel does not re-enter. Because `preUseActivity` fires after slot selection but before the placement crosshair, the old token, anchor, and "Summon:" AE are fully gone before the new token exists; midi-qol then pins the new token to a fresh AE nothing else is attached to. The `skipAnimation` option (new, in [modules/dismiss-flow.js](modules/dismiss-flow.js)) collapses the old teardown to fast database operations so no 15-second dismiss animation can race the crosshair. `onPostSummon` awaits the in-flight teardown via a `RECAST_DISMISS` map before building the new pipeline. Eight new regression tests for `findPreviousAnchor` in [tests/dismiss-helpers.test.mjs](tests/dismiss-helpers.test.mjs).

- **Duration-expiry dismissal threw a red banner and posted the wrong reason (smoke Bugs #2 and #3 — one root cause, two symptoms).** **Misdiagnosis correction first:** the v0.1.16 smoke report modeled Bug #2 as a token double-delete and said the banner id was a token id. Live capture with instrumented `.deleteDocuments()` wrappers proves the banner is an *anchor-AE* double-delete: the id `V2ZGiEC0FzqbMTwH` in `ActiveEffect "V2ZGiEC0FzqbMTwH" does not exist!` is the anchor AE id, not a token id. The actual delete sequence on duration expiry: both the dnd5e "Summon: Manifest Tulpa" AE and the module anchor "Manifest Tulpa (active)" carry `duration.seconds = 3600`; times-up expires both in a single batch. The Summon AE owns the Tulpa token as a `getDependents()` dependent. (s=1) times-up deletes the Summon AE; (s=2) dnd5e cascades and deletes the Tulpa token; (s=3) the token delete fires `onPreDeleteToken` → our hook deletes the anchor with `dismissReason:"manual"` (Bug #3 — stamps the wrong reason); (s=4) times-up's own scheduled anchor delete finds an already-removed id → red banner (Bug #2). The module's own guarded token delete (the `.has(id)` membership check) never fires on this path because `skipTokenTeardown:true` is set at s=3 — the banner is purely times-up's anchor double-delete. End state is always correct; both bugs were cosmetic/noisy, not functional. Fix: a new pure discriminator helper `isAnchorDurationExpired(anchor)` in [modules/dismiss-helpers.js](modules/dismiss-helpers.js) (`remaining != null && remaining <= 0`; pure, no Foundry globals, Node-testable). In `onPreDeleteToken` in [modules/dismiss-flow.js](modules/dismiss-flow.js), immediately after confirming the anchor exists: `if (isAnchorDurationExpired(anchor)) return;`. On duration expiry the anchor's `remaining ≤ 0` → we defer; times-up's s=4 delete becomes the sole anchor delete, and because it carries no module options, the funnel re-enters cleanly → `inferReason` step 5 returns `"duration"` (Bug #3 fixed) and there is no double-delete (Bug #2 fixed). A genuine GM manual delete has `remaining > 0` → guard false → the "manual" path is unchanged. The other four dismissal triggers (0 HP, caster death, recast, manual token delete) are unaffected: they are all driven by a direct anchor delete, so by the time any token falls the anchor is already gone and `onPreDeleteToken` early-returns at `if (!anchor) return;` before reaching the new guard. `inferReason`'s own step-5 check is now DRY, calling `isAnchorDurationExpired` instead of an inline comparison. Six new unit tests in [tests/dismiss-helpers.test.mjs](tests/dismiss-helpers.test.mjs) for `isAnchorDurationExpired` (remaining 0 → true; negative → true; 100/1 → false; null/undefined → false; `{}` → false; null/undefined anchor → false, never throws).

- **Dead-weight DAE `isDeath` specialDuration key removed from the anchor AE (smoke Bug #4).** The caster-side anchor "Manifest Tulpa (active)" carried `dae.specialDuration = ["zeroHP", "isDeath"]`. DAE 13.0.26 has no `isDeath` key (verified live via `DAE.daeSpecialDurations()`), so it was silently ignored — a no-op that misleadingly implied a death trigger existed. `zeroHP` already covers every realistic death (HP ≤ 0), so the minimal fix is to drop the dead key, not add a parallel death listener. The literal moved to a named constant `ANCHOR_SPECIAL_DURATIONS = ["zeroHP"]` in [modules/constants.js](modules/constants.js); [modules/cast-flow.js](modules/cast-flow.js) `createAnchorAE` now references it. Regression lock: new [tests/constants.test.mjs](tests/constants.test.mjs) asserts the list includes `zeroHP`, does NOT include `isDeath`, and is exactly `["zeroHP"]` — the dead key can't creep back.

- **Orphaned combatant left in the tracker after every dismissal (smoke Bug #6).** Foundry does not remove a combatant when its token is deleted — the combatant survives with `.token` resolving to `null` while `tokenId` still points at the deleted token. `performDismissCleanup` deleted the Tulpa token but never its combatant, so every dismissal left a ghost row in the combat tracker. Fix: a new pure helper `collectTulpaCombatants(combats, tokenId)` in [modules/dismiss-helpers.js](modules/dismiss-helpers.js) returns every combatant across all combats whose `tokenId` matches the dismissed Tulpa's token (read from the anchor's `tulpaTokenId` flag); [modules/dismiss-flow.js](modules/dismiss-flow.js) deletes them right after the token teardown. Pure and Node-testable (iterates with `for…of`, tolerating both live collections and plain arrays). Six new regression tests in [tests/dismiss-helpers.test.mjs](tests/dismiss-helpers.test.mjs) (null/undefined tokenId → []; null/undefined/empty combats → []; single match; no-match; across multiple combats; tolerates a combat with no combatants collection).

- **System "Summon:" AE leaked on a cancelled or over-budget cast (smoke Bug #7).** When the modification dialog is cancelled (or the chosen mods exceed the slot budget), the cast aborts before the anchor is ever created — but dnd5e/midi-qol has already created the "Summon: Manifest Tulpa" AE on the caster (it lands async ~2–4 s after the activity use, independent of our flow). The anchor-driven funnel never runs (there is no anchor), so the orphaned Summon AE stayed on the caster sheet. Fix: `abortAndCleanup` in [modules/cast-flow.js](modules/cast-flow.js) now also sweeps the system Summon AE via the existing `findSystemSummonAE(caster)` helper (name-regex match, since `flags.dnd5e.summon` is null on this AE), with both abort call sites passing the caster. The other (successful) path is unaffected — the anchor funnel already owns Summon-AE teardown there.

- **Over-budget dialog state made unreachable instead of merely rejected (smoke Bug #10).** Previously a player could tick mods past the slot budget; the dialog turned the counter "over" and disabled Confirm, but the over-budget state was still reachable and the counter rendered orange (a theme resolving `--color-text-hyperlink`), not as an error. Fix: [modules/cast-dialog.js](modules/cast-dialog.js) `_prepareContext` now computes the remaining budget before building rows and marks any not-yet-selected mod whose cost exceeds the remainder as `disabled`; [templates/cast-dialog.hbs](templates/cast-dialog.hbs) renders those checkboxes `disabled` and greys the row. Already-selected mods stay enabled so they can be unticked. The submit-time guard (`#onSubmit`) stays as defense in depth. CSS fix in [styles/manifest-tulpa.css](styles/manifest-tulpa.css): the over-budget counter is now an explicit error red (`#c0392b`), and the disabled-row rule targets `.mt-row.disabled` (the markup is `label.mt-row > input`, so the old `input:disabled + label` adjacent-sibling selector never matched).

- **`ready` console log printed before the double-registration guard (cosmetic).** [modules/init.js](modules/init.js) logged `manifest-tulpa | ready` at the top of the `ready` handler, so a double-load (e.g. a module reload) printed the line twice even though the guard then bailed. Moved the log below the `__manifestTulpaHooksRegistered` guard so it prints exactly once per genuine registration.

### Internal

- **Test count 78 → 85 → 93 → 99 → 108 across the four passes.** jb2a pass: 85/85 (+7 net, includes removing three green-but-broken animation-preset tests and adding seven new locks). Recast pass: 93/93 (+8 `findPreviousAnchor` tests). Duration-expiry pass: 99/99 (+6 `isAnchorDurationExpired` tests). Cleanup pass (Bugs #4/#6/#7/#10/cosmetic): 108/108 (+3 `ANCHOR_SPECIAL_DURATIONS` tests in the new `tests/constants.test.mjs`, +6 `collectTulpaCombatants` tests). `npm test` → **108 pass / 0 fail**. `npm run validate` → "Validation passed." `npm run build:packs` → "All packs built." (no `_source/` change across any pass; pack rebuild ran as a safety check only).

## [0.1.16] — 2026-05-27

> Content polish + regression armor for the shipped spell description. No module-code change. Restructures the spell's `system.description.value` from a 35-paragraph flat `<p>`-wall into a stat-block table, section-headed modification lists, and bold modification names. Adds two scoped `[[lookup ...]]{fallback}` enrichers (AC mod, HP formula) so the spell sheet shows live caster-derived numbers while the compendium browser still reads cleanly. Populates `system.description.chat` with a two-paragraph cast-card summary so the chat log no longer dumps the full spell text every cast. Locks all five new structural elements with regression assertions in [tests/spell-source.test.mjs](tests/spell-source.test.mjs) — the v0.1.13 silent-regression class can't recur on these fields.

### Spell description polish

- **`system.description.value` restructured.** Stat-block now lives in a 6-row `<table>` (AC, HP maximum, proficiency bonus, spell attack/save DC, STR/CON save proficiencies, Manifestation Strike damage). Modifications are grouped under six `<h4>` section headers (Morphic, Combat, Resistance, Movement, Skill Affinity, Special); named options are `<ul><li><strong>NAME</strong> (cost). Effect…</li>` bullets. Prose is verbatim from [manifest-tulpa.txt](manifest-tulpa.txt) — only structure is added.
- **Two scoped lookup enrichers added.** The AC table row uses `13 + [[lookup @attributes.spellmod]]{your spellcasting ability modifier} ([[13 + @attributes.spellmod]]{total})`; the HP row uses `[[40 + 5 * @details.level]]{40 + 5 × your character level}`. Both paths are already in use by the summon activity's `bonuses.ac` and `bonuses.hp` expressions, so resolution is verified in dnd5e 5.2.5. The `{fallback}` form ensures the compendium-browser view (no parent actor) renders prose, not raw `@`-paths.
- **`system.description.chat` populated.** Two-paragraph summary: stat block + cast-time choices in paragraph 1, dismissal triggers in paragraph 2. dnd5e renders `description.chat` in preference to `description.value` on cast chat cards (see [`item-data-model.mjs:198`](.understand-anything/dnd5e-research/dnd5e/module/data/abstract/item-data-model.mjs)), so the chat log stays tidy on each cast.

### Regression protection

- **Five new structure locks in [tests/spell-source.test.mjs](tests/spell-source.test.mjs).** Lock the `<table>` presence + AC/HP rows, the two enricher patterns, the six `<h4>` section headers, the bolded mod-name sample, and the `description.chat` constraints (non-empty, < 1500 chars, mentions Tulpa + dismiss). A future flatten, regenerate, or wipe fires the test suite before tag.
- **R36 header-needles check extended to `description.chat`.** The same header strings banned from `description.value` (R36, v0.1.11/v0.1.15) are now banned from `description.chat` too — same class of regression, second surface.
- **[CLAUDE.md](CLAUDE.md) field-lock table extended** with five new rows so the next agent in this codebase sees the locks without having to read the test file first. Appended a sentence under "Anti-pattern: rebuilding the scrub script" citing v0.1.16 as another precedent for direct `_source/` edits + same-commit test locks.

### Diagnostics

- **`projectile-explosion-light-yellow.webp` 404 investigated.** Diagnostic console snippet in [docs/superpowers/specs/2026-05-27-spell-description-polish-design.md](docs/superpowers/specs/2026-05-27-spell-description-polish-design.md) Section 1. Code audit confirmed no reference to that asset path in `_source/`, `modules/`, or `module.json`. R49 in the v0.1.16 smoke report documents the runtime finding.

## [0.1.15] — 2026-05-27

> Hotfix for v0.1.13 — but the root cause turned out to be much worse than the visible R36 symptom suggested, and the fix is structural rather than line-level. **v0.1.13 silently regressed eight shipped-spell fields**, not one. The earlier framing in this changelog draft ("patch the generator, add tests") was wrong: the generator itself was the bug. This release **deletes [scripts/scrub-source.mjs](scripts/scrub-source.mjs) entirely**, restores the eight regressed fields from v0.1.12 (the last known-good shipped artifact), moves the pre-development `fvtt-*.json` world exports to [`archive/`](archive/) so the next person sees a `README.md` warning instead of an enticing "scrub input" file, locks every previously-silent field with regression tests, and rewrites the [CLAUDE.md](CLAUDE.md) standing order to reflect the new reality (`_source/` is hand-edited canonical JSON; no generator exists; `fvtt-*` files are historical and must not be parsed). The v0.1.13 release process re-ran `npm run scrub` to bake in the new icon path, and the scrub blindly copied every field it didn't explicitly canonicalize from a pre-development world export whose values had been wrong since v0.1.2 (wrong range, wrong target type, wrong material component, wrong consumption flag, plus the v0.1.11 R36 header fix). The "fix-at-generator-layer" principle (v0.1.11→v0.1.13 R36 scar) still applies — but when the generator is what re-reads bad inputs, the fix is to delete the generator.

### Fixed

- **Eight silently regressed shipped-spell fields restored (CRITICAL — v0.1.13 silent regression scope).** A `git diff v0.1.12 -- _source/manifest-tulpa-spells/Item.manifest-tulpa.json` against the v0.1.13 release confirmed the scrub re-write clobbered every field it didn't explicitly canonicalize. Restored from `v0.1.12` and re-applied the v0.1.13 icon path on top:
  - `system.range.value`: `null` → `30` (spell text says "Range 30 feet"; v0.1.13 lost it because the pre-dev export had `units: "self"`)
  - `system.range.units`: `"self"` → `"ft"`
  - `system.target.affects.type`: `"self"` → `"space"` (a 30-ft self-targeted spell is incoherent; dnd5e's slot UI was rendering the wrong target shape)
  - `system.materials.value`: pre-dev "a lock of your own hair, a drop of your blood, and a small gem worth 100 GP" → canonical "a crystal shard imbued with your psychic resonance, worth at least 100 GP" (the spell text was rewritten during development; the pre-dev export was never updated)
  - `system.materials.consumed`: `true` → `false` (the spell does not consume the shard — re-castable across the day with the same focus)
  - `system.materials.cost`: missing → `100` (matches the GP value declared in `materials.value`)
  - Summon-activity `range.override`: `false` → `true` (without override, the activity inherits dnd5e's default summon range, not the spell's 30 ft)
  - Summon-activity `target.affects.type`: stripped entirely → `"space"` with `target.override: true`
  - **Also**: the R36 header-duplication fix from v0.1.11 (`description.value` opens with `<p>You crystallize a fragment...`, no Level 5 / Casting Time / Range / Components / Duration header block) was restored at the same time, since v0.1.13's scrub had silently re-baked it in too — this is the symptom the v0.1.13 smoke surfaced and that originally kicked off this release.
- **R36 (spell description duplicates the front-matter header)** is fixed as part of the eight-field restore above. The header block lived inside the broader silent-regression set, not as a standalone bug.

### Removed

- **`scripts/scrub-source.mjs` deleted.** The script's design was the bug: it took pre-development `fvtt-Actor-tulpa-*.json` and `fvtt-Item-manifest-tulpa-*.json` world exports as authoritative input, applied a handful of canonicalizations on top (icon path, identifier, summon profile UUID, rules era, scaling-allowed flag), and wrote everything else through verbatim — including all eight regressed fields above. The pre-dev exports were stale from the moment the spell text was rewritten in early development; every shipped artifact built from them inherited the staleness, masked only because the validator and tests didn't cover the un-canonicalized fields. There is no replacement script. `_source/manifest-tulpa-spells/Item.manifest-tulpa.json` and `_source/manifest-tulpa-actors/Actor.tulpa.json` are now hand-edited canonical JSON; future spell-shape changes happen by editing those files directly and running `npm test && npm run validate && npm run build:packs`.
- **`"scrub": "node scripts/scrub-source.mjs"` removed from `package.json` scripts block.** `npm run scrub` no longer exists; quick-command callers will get an `npm ERR! missing script` instead of a silent re-clobber.
- **Pre-development `fvtt-*.json` world exports moved from repo root to [`archive/`](archive/).** Six files: `fvtt-Actor-tulpa-rfi8EPvTDFduYlW5.json`, `fvtt-Item-manifest-tulpa-YwUNZpFtX3dwNQPx.json`, `fvtt-ActiveEffect-harrowing-presence.json`, and three `fvtt-Macro-*.json` (the macros are superseded by `modules/*.js` per architectural invariant 4). Added [`archive/README.md`](archive/README.md) explaining the files are pre-v0.1.0 brainstorming exports, have been fundamentally wrong since v0.1.2, and must not be parsed by any build/validate/test/release tool. They remain in-tree only for git-history grep-ability.

### Added

- **[tests/spell-source.test.mjs](tests/spell-source.test.mjs) — 14 regression locks for every previously-silent shipped-spell field.** Replaces the v0.1.15-draft `tests/scrub-spell.test.mjs` (which tested the now-deleted generator's `stripFrontMatter` helper). The new tests assert directly against the shipped `_source/` files — the canonical artifact — rather than against any generator output. Locks:
  - `_id` `manifesttulpaS01` (spell) and `manifesttulpaA01` (actor) — the 16-char canonical ids the summon profile UUID points at (Foundry id-format scar from v0.1.2).
  - `system.identifier` `manifest-tulpa` — the locale-safe key [modules/cast-flow.js](modules/cast-flow.js) gates on at `preUseActivity`.
  - `system.source.rules` `"2024"` — declares the dnd5e 2024 rules era.
  - `img` `modules/manifest-tulpa/assets/manifest_tulpa_icon.webp` — the v0.1.13 icon path, preserved through the restore.
  - **v0.1.13 silent-regression locks** for the eight fields above: `range.value`/`range.units`, `target.affects.type`, `materials.value` (must contain "crystal shard" and "100 GP", must NOT contain the pre-dev "lock of hair"/"drop of blood" strings), `materials.consumed: false`, `materials.cost: 100`, summon-activity `range.override: true`, summon-activity `target.affects.type: "space"` with `target.override: true`, summon profile UUID, `consumption.scaling.allowed: false`.
  - **R36 description-header lock** — `description.value` contains none of "Level 5 Conjuration" / "Casting Time:" / "Range: 30 feet" / "Components:" / "Duration: 1 Hour" and opens with `<p>You crystallize a fragment`.
  - **Anti-generator lock** — `existsSync("scripts/scrub-source.mjs")` must be `false`. The inline failure message explains why the script was deleted and what to do instead (one-shot scripts under `scripts/` must be deleted after use).
- **`"Source-tree discipline — STANDING ORDER"` section in [CLAUDE.md](CLAUDE.md).** Replaces the previous "Generator/output discipline" section, which was itself a v0.1.15-draft response to R36 and assumed a generator existed. New section spells out: (a) `_source/` is hand-edited canonical JSON and no generator regenerates it from `fvtt-*` exports; (b) `archive/` files are pre-v0.1.0 historical reference and must not be read by build/validate/test/release tooling; (c) the three-step rule for any shipped-artifact change (edit `_source/` directly → add or update an assertion in `tests/spell-source.test.mjs` or `scripts/validate-pack.js` → run `npm test && npm run validate && npm run build:packs`); (d) the full locked-fields table mapping each field to its test or validator.

### Internal

- **Test count 57 → 71 (+14 net).** Removed: scrub-spell.test.mjs draft (uncommitted, never shipped). Added: spell-source.test.mjs (14 cases). All 71 cases pass under `node --test`.
- **`modules/cast-flow.js` comment refresh.** Line 46 used to read `// The scrub script (scrub-source.mjs) sets system.identifier; localization-safe.`; now reads `// _source/manifest-tulpa-spells/Item.manifest-tulpa.json carries this identifier; locale-safe.` — no behavioral change, just removes a stale generator reference that would mislead future readers.
- **`README.md` quick commands no longer list `npm run scrub`.**
- **Architectural invariant 4 in CLAUDE.md updated** to reference `archive/` instead of the repo root for the `fvtt-Macro-*.json` files.
- **`package.json` version stays in sync with `module.json`.** Both at `0.1.15`.

## [0.1.14] — 2026-05-27

> Hotfix for v0.1.13. The custom spell icon shipped in v0.1.13 404'd at runtime (`GET /modules/manifest-tulpa/assets/manifest_tulpa_icon.webp 404 (Not Found)`) and the spell rendered with Foundry's broken-image glyph. Root cause: the release workflow's zip step ([.github/workflows/release.yml](.github/workflows/release.yml)) listed `modules/ styles/ lang/ templates/ packs/` but not `assets/`, so the icon file was never bundled into `manifest-tulpa.zip` even though it landed in git and `_source/manifest-tulpa-spells/Item.manifest-tulpa.json` referenced it. No runtime code changes — workflow-only fix plus the version bumps the release pipeline needs.

### Fixed

- **Bundle `assets/` into the release zip.** The `Zip module bundle` step in [.github/workflows/release.yml](.github/workflows/release.yml) now appends `assets/` to its directory list so `manifest_tulpa_icon.webp` (and any future bundled art) actually reaches the user's `modules/manifest-tulpa/` install directory. Verified locally that the zip step pattern matches the runtime path the spell JSON references. Smoke check R45 (icon visible) should now PASS where v0.1.13 would have FAILed end-to-end despite the source change being correct.

### Internal

- **`package.json` version stays in sync with `module.json`.** Both at `0.1.14`.

## [0.1.13] — 2026-05-27

> Addresses **every** finding from the v0.1.12 smoke-test report ([docs/superpowers/test-plans/2026-05-27-v0.1.12-smoke-report.md](docs/superpowers/test-plans/2026-05-27-v0.1.12-smoke-report.md)) — the CRITICAL REG-1 (`dnd5e.postUseActivity` HOOK3 silently dropped → slot consumed, no Tulpa placed), the HIGH REG-2 (recast crashed at [modules/cast-flow.js:99](modules/cast-flow.js#L99) with `Error: undefined id` because the dismiss cascade was fire-and-forget while the new cast pipeline raced ahead), and the LOW A7 (`dismiss animation timed out after 5000ms` warning fired on healthy JB2A assets whose `.waitUntilFinished(-200)` window comfortably exceeds 5 s). Also packages the user's custom spell icon. **Pack rebuild required** because `_source/manifest-tulpa-spells/Item.manifest-tulpa.json` changed (icon path); `npm run build:packs` ran as part of the release. The CI release workflow will re-run it on tag-push.

### Added

- **Custom spell icon shipped (user-supplied art).** The spell previously used dnd5e's generic `systems/dnd5e/icons/svg/items/spell.svg` placeholder. Bundled the user's `manifest_tulpa_icon.webp` at [assets/manifest_tulpa_icon.webp](assets/manifest_tulpa_icon.webp) and pointed [_source/manifest-tulpa-spells/Item.manifest-tulpa.json](_source/manifest-tulpa-spells/Item.manifest-tulpa.json) `img` at `modules/manifest-tulpa/assets/manifest_tulpa_icon.webp` (the install-time module-relative path Foundry resolves at runtime). Also updated [scripts/scrub-source.mjs](scripts/scrub-source.mjs) `scrubSpell` to canonicalize `s.img` to the same path so a future re-scrub from the world export can't silently revert it. The anchor AE in [modules/cast-flow.js](modules/cast-flow.js) already inherits the spell-item img via `caster.items.find(i => i.system?.identifier === SPELL_IDENTIFIER)?.img`, so the new icon also shows on the caster-side "Manifest Tulpa (active)" effect for the full cast duration without any further change.

### Fixed

- **`dnd5e.postUseActivity` silently dropped → slot consumed, no Tulpa, no dialog (REG-1, CRITICAL).** v0.1.12's smoke session was unable to land a first cast at all: the spell-slot decremented, the chat card posted, but `manifest-tulpa | postUseActivity HOOK3 fired` never appeared in the console — the module's handler never ran. Root cause is in Foundry's hook dispatcher: `Hooks.call("dnd5e.postUseActivity", …)` short-circuits on the first listener that returns `false`. The user's load order has four listeners on that hook string (substance/addiction HOOK0/HOOK1, AutoAnimations HOOK2, our HOOK3); a `return false` from any earlier handler silently blocks every later one. The smoke proved this with an MT-WRAP `libWrapper` wrap on `SummonActivity.prototype.use` that observed `postUseActivity` firing for HOOK0–HOOK2 only. Fixed by moving the entry point to **`dnd5e.postSummon`** ([.understand-anything/dnd5e-research/dnd5e/module/documents/activity/summon.mjs:222](.understand-anything/dnd5e-research/dnd5e/module/documents/activity/summon.mjs#L222)) which uses `Hooks.callAll` — short-circuit-immune — and fires from both the inline-placement path (`_finalizeUsage` → `placeSummons`) and the chat-card "Summon" button path (`#placeSummons` static → same `placeSummons`). Signature is `(activity, profile, createdTokens, options)`, so `createdTokens[0]` replaces the old `results.summoned[0]` read and the entire `locate-helpers.js` indirection (canvas-scan fallback, summoned-vs-createdTokens key checks) is gone. The slot level — formerly read from `usageConfig.spell.slot` in `postUseActivity` — is now captured at **`dnd5e.preUseActivity`** (which is proven to fire by the smoke's "slot consumed" observation) into a small `SLOT_CAPTURE` Map keyed by `activity.uuid` and consumed-and-cleared in `postSummon` on the same key. A chat-button path with no preUseActivity capture falls back to slot 5 (the spell's base level). Exported `onPreUseActivity` and `onPostSummon` from [modules/cast-flow.js](modules/cast-flow.js); [modules/init.js](modules/init.js) registers the new pair and drops `dnd5e.postUseActivity`. Renamed the handler to make the new contract obvious. **CLAUDE.md invariant #2 updated** to reflect the new entry-points.
- **Recast crashed with `Error: undefined id [GRhO38Y2RLiYL8lj]` (REG-2, HIGH).** v0.1.12's recast path awaited only `previous.delete(...)`, but the downstream `deleteActiveEffect` hook (which runs the actual cascade — token delete + animation + system-AE sweep + chat card) is fire-and-forget from Foundry's perspective. The cast pipeline raced ahead into Phase 1 (dialog open), the user picked modifications, and Phase 3 (anchor creation, chat card) ran while the previous dismiss was still in flight. The crash surfaced inside `chat-cards.js#postCast`'s `ChatMessage.getSpeaker({ actor })` call, which touches the speaker's token/actor identity at the exact moment the previous Tulpa's token was mid-delete. Fixed by extracting the body of `onDeleteActiveEffect` in [modules/dismiss-flow.js](modules/dismiss-flow.js) into a new exported `performDismissCleanup(effect, options)` function and calling it directly from the recast branch in [modules/cast-flow.js](modules/cast-flow.js) — awaiting the full teardown (animation, token delete, system-AE sweep, chat card) before continuing. The anchor itself is then deleted with `{ [MODULE_ID]: { skipFunnel: true } }` so the hook handler no-ops on its own delete event (it would otherwise re-run the cleanup against an already-gone tulpaUuid). `onDeleteActiveEffect` now short-circuits at the top when `options[MODULE_ID]?.skipFunnel === true`. The five non-recast triggers (duration / Tulpa-0HP / caster-0HP / caster-dead / GM token-delete / GM AE-delete) still flow through the funnel unchanged because none of them pass `skipFunnel`.
- **Dismiss animation timed-out warning fired on healthy assets (A7, LOW, carried from v0.1.11).** [modules/animations.js](modules/animations.js) `withTimeout` capped every animation Promise at 5 000 ms; the dismiss path uses `.waitUntilFinished(-200)` on JB2A assets whose intrinsic playback can exceed 5 s for the longer presets, so the safety timeout fired on every successful dismissal and printed a spurious `manifest-tulpa | animation dismiss failed: Error: dismiss animation timed out after 5000ms` console warning. Raised `ANIM_TIMEOUT_MS` from 5 000 to 15 000 ms (still well under any reasonable user attention budget for a stuck-animation safety net, comfortably clears every shipped dismiss preset, preserves the same fall-through-on-missing-asset behavior). Pre-flight `Sequencer.Database.entryExists` continues to short-circuit before the timeout window opens for genuinely missing assets, so the worst case for a stuck animation is still bounded.

### Internal

- **`modules/locate-helpers.js` and `tests/locate-helpers.test.mjs` deleted.** Their only consumer was `locateSummonedTulpa` in [modules/cast-flow.js](modules/cast-flow.js), which existed to read `results.summoned` and fall back to a canvas placeable scan. The `dnd5e.postSummon` migration (REG-1) hands the TokenDocument[] directly to our handler, so both helpers are dead code. Test count drops from 66 → 58.
- **`package.json` version stays in sync with `module.json`.** Both at `0.1.13`.

## [0.1.12] — 2026-05-27

> Addresses **every** item the v0.1.11 smoke-test report surfaced ([docs/superpowers/test-plans/2026-05-27-v0.1.11-smoke-report.md](docs/superpowers/test-plans/2026-05-27-v0.1.11-smoke-report.md)) — the two medium-severity bugs, both LOW observations, and the v0.1.10 Bug 4 carryover. v0.1.11 confirmed that the "Summon: Manifest Tulpa" system AE *does* in fact persist on the caster after dismissal (contradicting the speculative v0.1.11 changelog note that suggested it was a misread) — Bug 1 carries forward from v0.1.9 and is now fixed. v0.1.11 also surfaced a new orphan-token bug (Bug 2): cancelling the modification dialog after the Tulpa had been placed left the token on the canvas; smoke-session accumulated 5 orphans. Observation 2 turned out to be a real root cause, not "investigation pending": the module bet on a `dnd5e.combatTurnStart` hook string that dnd5e never emits, so the Harrowing save never auto-fired from `combat.nextTurn()` — switched to `dnd5e.combatRecovery` filtered on `periods.includes("turnStart")`, which is the canonical dnd5e 5.2.5 signal. The v0.1.10 Bug 4 carryover (HP aura 10ft vs spell text) was verified as a false positive: [manifest-tulpa.txt:34](manifest-tulpa.txt#L34) RAW reads "10-foot aura" and the registry's `radius: "10"` already matches — no code change needed. No `_source/` changes, no pack rebuild required.

### Fixed

- **Cancel-after-placement left an orphan Tulpa on the canvas (Bug 2, medium, new in v0.1.11).** `locateSummonedTulpa` in [modules/cast-flow.js](modules/cast-flow.js) was reading `results?.createdTokens?.[0]` from the `dnd5e.postUseActivity` hook payload. In dnd5e 5.2.5, `SummonActivity._finalizeUsage` writes the created `TokenDocument[]` to `results.summoned`, not `results.createdTokens` (see system source `module/documents/activity/summon.mjs` ~L113) — so the primary path was always undefined and the canvas-scan fallback (which itself wasn't reliable across abort paths) was the only thing standing between the user and an orphan. Fixed by extracting `pickSummonedFromResults(results)` and `scanPlaceablesForSummon(placeables, casterUuid)` into [modules/locate-helpers.js](modules/locate-helpers.js) (pure, no Foundry globals, follows the `modification-registry.js` Node-testable pattern), reading the canonical `results.summoned` key first, and moving the locate call to run *before* the dialog opens so the cancel branch always has a TokenDocument handle to delete. Also normalised `locateSummonedTulpa` to return a TokenDocument in both paths (the canvas-scan fallback's placeable is unwrapped via `.document`), and switched `abortAndCleanup` from `token.document.delete()` to `token.delete()` since we're now dealing with a TokenDocument directly. Sequencer's `.atLocation` accepts both Token and TokenDocument so `playManifest` is unaffected. Eight new unit tests in [tests/locate-helpers.test.mjs](tests/locate-helpers.test.mjs) lock the helper behaviour: primary key read, stale `createdTokens` key explicitly rejected, fallback-scan tolerates missing actor/flag/createdTime, most-recent ordering, and casterUuid prefix match.
- **Orphaned dnd5e system `Summon: Manifest Tulpa` AE on the caster (Bug 1, medium, carried from v0.1.9).** The single-funnel dismissal in [modules/dismiss-flow.js](modules/dismiss-flow.js) only removed the module's anchor AE — the dnd5e-system-created summon AE (named "Summon: Manifest Tulpa" and carrying `flags.dnd5e.summon.origin` pointing at the spell item) persisted on the caster indefinitely until manual deletion. v0.1.11's changelog speculated this might be a tester misread; the v0.1.11 smoke session conclusively reproduced it across multiple cycles. Fixed by adding `findSystemSummonAE(caster, spellIdentifier)` to [modules/dismiss-helpers.js](modules/dismiss-helpers.js) — a pure helper that resolves the spell item via `caster.items.find(i => i.system.identifier === "manifest-tulpa")` (canonical localization-safe lookup) and matches AEs whose `flags.dnd5e.summon.origin` either equals the spell item UUID or starts with `${spellItemUuid}.` (covers activity-suffixed origin strings); a name-regex fallback (`/\bmanifest\s*tulpa\b/i`) provides defense-in-depth for the case where the spell item can't be resolved. The dismiss flow now sweeps the system AE right before posting the dismiss chat card, so every dismissal path (duration / 0 HP / death / recast / manual delete) cleans up both AEs atomically. Nine new unit tests in [tests/dismiss-helpers.test.mjs](tests/dismiss-helpers.test.mjs) cover UUID prefix match, exact UUID equality, name-regex fallback, hyphen/whitespace tolerance, custom spellIdentifier, and explicit non-matches for unrelated summon spells.
- **Harrowing Presence save never auto-fired on turn start (Observation 2, LOW → root-cause root-fix).** The smoke report described the `dnd5e.combatTurnStart` hook as "not auto-firing from `combat.nextTurn()`" and held it as investigation-pending. Investigation: dnd5e 5.2.5 emits no such hook anywhere in its codebase — a `Hooks.callAll("dnd5e.combatTurnStart", …)` string does not exist (verified against [.understand-anything/dnd5e-research/dnd5e/module/](.understand-anything/dnd5e-research/dnd5e/module/), including the `Combat5e._onStartTurn` and `Combatant5e.recoverCombatUses` paths that fire on turn transitions). v0.1.11 picked a hook name that simply never gets called, so the marker-AE → save path was effectively dormant: the AA-propagated marker landed on hostiles correctly, but the imperative half (roll Wis save, apply `frightened`) never ran. Switched the listener in [modules/harrowing-presence-hook.js](modules/harrowing-presence-hook.js) to `dnd5e.combatRecovery` — fired by `Combatant5e.recoverCombatUses(periods)` from `Combat5e._onStartTurn` (see [.understand-anything/dnd5e-research/dnd5e/module/documents/combatant.mjs:124](.understand-anything/dnd5e-research/dnd5e/module/documents/combatant.mjs#L124)). The hook signature is `(combatant, periods, results)`; we gate on `periods.includes("turnStart")` to ignore the every-combatant `turn`-period firings and only act for the combatant whose turn is actually starting, then read the actor via `combatant.actor`. Function renamed `onCombatTurnStart` → `onCombatRecovery`; [modules/init.js](modules/init.js) wires `Hooks.on("dnd5e.combatRecovery", onCombatRecovery)` in place of the dead string.

### Added

- **ActiveAuras `combatOnly` setup gotcha documented (Observation 1, LOW).** The smoke report flagged that the world setting `ActiveAuras.combatOnly`, when `true`, gates all aura propagation behind an active combat — including the Harrowing Presence marker — and that no install instruction warned about it. [README.md](README.md) now lists `ActiveAuras 0.12.7+` in Required modules (the section was stale from the v0.1.10 → v0.1.11 propagation-engine swap and still named `auraeffects 1.5.2+`; `lib-wrapper` and `socketlib` were also missing) and adds a "Setup notes" section explaining that, with combatOnly enabled, the aura only applies once a combat is active. [modules/init.js](modules/init.js)'s `ready` listener now reads `game.settings.get("ActiveAuras", "combatOnly")` and emits a single `manifest-tulpa | note: ActiveAuras.combatOnly is enabled — Harrowing Presence will only propagate while a combat is active.` info-level log when the setting is on, so the gotcha surfaces in-world too. The `try/catch` swallows the failure path when AA's settings aren't yet registered (e.g. AA not installed).

### Verified (no-op)

- **Harrowing Presence radius matches RAW (v0.1.10 Bug 4 carryover, LOW).** The v0.1.10 smoke filed a 10ft vs 15ft mismatch between the registry and the spell text. RAW says "10-foot aura" ([manifest-tulpa.txt:34](manifest-tulpa.txt#L34)) and `harrowingPresence.build` already sets `radius: "10"` ([modules/modification-registry.js:237](modules/modification-registry.js#L237)). Closed as already-correct; the v0.1.10 filing read the spell text wrong (likely confused with a different aura modification's range). No code change.

### Internal

- **Two new pure-helper modules.** [modules/locate-helpers.js](modules/locate-helpers.js) and [modules/dismiss-helpers.js](modules/dismiss-helpers.js) follow the established `modification-registry.js` "pure data + pure helper functions; no Foundry globals" pattern, which keeps the bug-fix logic out of the integration-heavy flow modules and makes the predicates regression-locked at the `node --test` layer rather than relying solely on Foundry smoke testing.
- **Test suite grew from 49 → 66 cases (+17).** Eight tests for `locate-helpers`, nine for `dismiss-helpers`. Total Node test runtime stays under 100 ms.
- **`package.json` version stays in sync with `module.json`.** Both at `0.1.12`.

## [0.1.11] — 2026-05-27

> Addresses every actionable finding from the v0.1.10 smoke-test report ([docs/superpowers/test-plans/2026-05-26-v0.1.10-smoke-report.md](docs/superpowers/test-plans/2026-05-26-v0.1.10-smoke-report.md)). The headline fix is Bug 3 (HIGH): Harrowing Presence's marker AE has been broken across every version since v0.1.6 because Aura Effects 1.5.2's V13 registration path for AE-typed aura sources on *synthetic/unlinked* actors never landed the marker on hostiles — the aura visual rendered, but no propagation event ever fired. v0.1.11 swaps propagation engines to **Active Auras** (kandashi 0.12.7, V13-verified) without rewriting the `combatTurnStart` hook. The remaining three cosmetic bugs from the report are also fixed: the modification picker now has a scrollable body with a pinned footer (Bug 5 — the user identified this as the highest-priority cosmetic issue), the dismiss chat card no longer says "the token was removed manually" when only the anchor effect was deleted (Bug 1), and the spell card description no longer double-prints the "Manifest Tulpa / Level 5 Conjuration / Casting Time… Duration: 1 Hour" header that dnd5e already renders from structured fields (Bug 4). Smoke-test plan extended with rows R33–R36. **Pack rebuild required** for Bug 4 — `npm run build:packs` ran as part of this release. Smoke-report Bug 2 (orphaned "Summon: Manifest Tulpa" system AE) is held for v0.1.11 user-retest: research against dnd5e 5.2.5 confirms the system only creates a caster-side AE for *concentration* spells (`Concentrating: <spell>`), not for non-concentration summons like Manifest Tulpa, so the tester's observation may have been a misread — but the scrollbar fix unblocks the user from completing the retest cycle. Smoke-report Bug 4 (aura distance 10 vs claimed-15) is a false positive: [manifest-tulpa.txt:34](manifest-tulpa.txt#L34) RAW reads "10-foot aura", which the registry already matches.

### Fixed

- **Harrowing Presence aura never propagated the marker to in-range hostiles (Bug 3, high).** The user's v0.1.10 smoke test placed a hostile NPC at 5 ft from a Tulpa with Harrowing Presence selected, started combat, advanced turns, moved tokens in and out of range, called `migrateActiveAuras()` explicitly — and the NPC's effects array remained empty across every test. The aura visual rendered, the `auraeffects.aura` document on the Tulpa had every required field (`collisionTypes: ["move"]`, `disposition: -1`, `script: "true"`, the two flag-key `changes`), and Aura Effects 1.5.2 recognized the effect as an aura (rendering proves it). The failure was in Aura Effects' propagation step for AE-typed sources on *synthetic/unlinked* actors — the Tulpa's aura AE lives on a token delta, not a world actor, and Aura Effects' V13 build never registered the source for proximity scanning. v0.1.11 swaps propagation engines to **Active Auras** 0.12.7 (kandashi, V13-verified, already in the user's stack), which the paladin in a parallel game uses successfully for Aura of Protection on a similar setup. AA propagates by cloning the entire effect onto in-range tokens via `foundry.utils.duplicate`, so the existing flag-key `changes` array (`flags.manifest-tulpa.inHarrowingAura = "true"`, `flags.manifest-tulpa.auraDC = "<dc>"`) and the literal `flags["manifest-tulpa"].auraDC` numeric riding alongside both land on each hostile target — and the existing `dnd5e.combatTurnStart` hook in [modules/harrowing-presence-hook.js](modules/harrowing-presence-hook.js) reads `actor.getFlag(MODULE_ID, "inHarrowingAura")` and `auraDC` exactly as before, no rewrite needed. Rewrote `harrowingPresence.build` in [modules/modification-registry.js](modules/modification-registry.js) to return a plain ActiveEffect (no `auraeffects.aura` type) tagged with `flags.ActiveAuras = { isAura: true, aura: "Enemy", radius: "10", ignoreSelf: true, hostile: true, collisionTypes: ["move"], wallsBlock: "system", time: "None", ... }`. The DC is resolved at cast time from the *caster's* `system.attributes.spell.dc` (with a `spelldc` fallback) and written as a literal string into the `changes` value — not `@attributes.spelldc`, which would resolve against the *Tulpa's* prepared data when AA clones the effect onto the hostile target. [modules/cast-flow.js](modules/cast-flow.js)'s `aura+marker` branch still calls `tulpa.createEmbeddedDocuments("ActiveEffect", [aura])` — that's the correct AA pickup path. Two unit tests in [tests/modification-registry.test.mjs](tests/modification-registry.test.mjs) lock the new shape: one asserts the effect type is *not* `auraeffects.aura` and the two flag-key changes still carry the literal numeric DC, the other asserts the full `flags.ActiveAuras` schema block (radius "10", aura "Enemy", hostile, ignoreSelf, `collisionTypes: ["move"]`) plus the foreign-namespace `flags["manifest-tulpa"].auraDC` numeric. `module.json` swaps `auraeffects` out of `requires` and adds `ActiveAuras` (camelCase, no hyphen — verified upstream), `socketlib` (lowercase), and `lib-wrapper` (hyphenated lowercase, despite display name "libWrapper") which AA requires.
- **Modification picker had no scrollbar; submit/cancel buttons fell off the bottom of the viewport (Bug 5, cosmetic — user-flagged top priority).** The cast dialog's modification picker now lists 8 Morphic entries (including 5 size shifts), 4 Combat, 10 Resistance, 4 Movement, 18 Skill, and 1 Special — on a standard viewport that extends well past the visible screen area, and `ApplicationV2` `position: { height: "auto" }` lets the form grow unbounded with no scroll indicator. The "Manifest Tulpa" submit button was reachable only by mouse-wheel scrolling inside the dialog. Fixed in [styles/manifest-tulpa.css](styles/manifest-tulpa.css) by making `.mt-cast-body` a `flex: column` container with `max-height: 70vh`, the inner `<section>` `flex: 1 1 auto; overflow-y: auto`, and the inner `.form-footer` (slot counter + Cancel/Manifest buttons) `flex: 0 0 auto` with a top border — so the body scrolls, the footer stays pinned, and the dialog itself never grows past 70% of the viewport. No JS changes; ApplicationV2 still sizes the outer window to `auto`, but the inner body now caps itself.
- **Dismiss chat card said "the token was removed manually" when only the anchor effect was deleted (Bug 1, cosmetic, carried forward from v0.1.9).** When a player or GM right-click → Delete'd the "Manifest Tulpa (active)" anchor AE from the caster's effects panel — *not* the token — the dismiss cascade ran correctly (token removed, both AEs cleaned up) but the chat card read "the token was removed manually," which is the opposite of what happened. Root cause in [modules/dismiss-flow.js](modules/dismiss-flow.js)'s `inferReason` ladder: the final fallback was `return "manual"`, and `lang/en.json` mapped `manual` to "the token was removed manually." The fallback is reached for any anchor-deletion path that isn't tagged via `options[MODULE_ID].dismissReason` (recast/Tulpa-zeroHP/manual-token-delete) and doesn't match the caster-dead, caster-zero-HP, or duration-expired branches — which includes the GM right-click case. Split the two paths: `inferReason` now returns `"anchorRemoved"` for the unflagged-fallback case, and the `onPreDeleteToken` hook (trigger #5 — GM right-click delete of the *token*) continues to pass `dismissReason: "manual"` explicitly. [lang/en.json](lang/en.json) updates `manual` to "the Tulpa's token was deleted" and adds a new `anchorRemoved` → "the anchor effect was removed" entry. Now the chat card describes the correct trigger regardless of which entity the GM clicked.
- **Spell card description double-printed the front-matter header (Bug 4, cosmetic).** dnd5e renders the spell's level/school/casting-time/range/components/duration natively from `system.level`, `system.activation`, `system.range`, `system.materials`, `system.duration` — and then displayed the literal "Manifest Tulpa / Level 5 Conjuration / Casting Time: Action / Range: 30 feet / Components: V, S, M (...) / Duration: 1 Hour" header *again* because the same lines were baked into the top of `system.description.value` during the original world-export scrub. Stripped the leading header block from [_source/manifest-tulpa-spells/Item.manifest-tulpa.json](_source/manifest-tulpa-spells/Item.manifest-tulpa.json); the description now starts directly with "You crystallize a fragment of your mental essence…" matching the prose in [manifest-tulpa.txt](manifest-tulpa.txt). Re-ran `npm run build:packs` to apply the change to the LevelDB pack — this is the first `_source/` change since v0.1.7, so the pack rebuild is part of the release.

### Added

- **Compendia now live in an orange "Tulpa Compendia" folder.** Previously the two packs (`Manifest Tulpa - Spells`, `Manifest Tulpa - Actors`) appeared as siblings of every other module's compendia in the Foundry Compendium Packs sidebar, making them harder to locate at a glance in a crowded module list. [module.json](module.json) now declares a `packFolders` entry that groups both packs under a single folder named "Tulpa Compendia" with `color: "#FF6400"` (orange) and `sorting: "m"` (manual). No pack rebuild required — `packFolders` is a manifest-level declaration; Foundry V13 reads it at world load and renders the folder + nests the packs inside on the next sidebar refresh.

### Internal

- **Active Auras replaces Aura Effects as the aura-propagation engine.** No other modification uses an aura, so this swap is contained. The two-stage Harrowing Presence architecture (source effect on Tulpa → propagated marker on hostiles → `combatTurnStart` hook reads marker, rolls save, applies `frightened`) is unchanged; only the propagation layer differs. [module.json](module.json) `requires` now lists `ActiveAuras` ≥ 0.12.7, `socketlib`, and `lib-wrapper`; `auraeffects` is dropped. The `combatTurnStart` hook in [modules/harrowing-presence-hook.js](modules/harrowing-presence-hook.js) is unchanged — `actor.getFlag(MODULE_ID, "inHarrowingAura")` and `actor.getFlag(MODULE_ID, "auraDC")` resolve identically against AE-typed and AA-cloned marker effects because both write to actor prepared data via flag-key `changes`.
- **Pack rebuild ran as part of this release.** v0.1.7 through v0.1.10 shipped without `_source/` changes, so the LevelDB pack was untouched. v0.1.11 modifies `Item.manifest-tulpa.json`, so `npm run build:packs` must run before tagging — see Bug 4. The CI release workflow already runs `build:packs` before zipping, so a clean `v0.1.11` tag-and-push handles this automatically; the manual build here keeps the in-repo `packs/` directory consistent for development.
- **`package.json` version stays in sync with `module.json`.** Both at `0.1.11`.

## [0.1.10] — 2026-05-26

> Addresses every actionable finding from the v0.1.9 smoke-test report ([docs/superpowers/test-plans/2026-05-26-v0.1.9-smoke-report.md](docs/superpowers/test-plans/2026-05-26-v0.1.9-smoke-report.md)). v0.1.9 merged `setStrikeDamageType` and `empoweredStrikes.patch` into a single-writer (`applyStrikeChanges`) — the right call architecturally — but the implementation used `foundry.utils.deepClone` against a `Set`-typed `DamageData.types` and silently lost every damage-type write. Live-play also surfaced a token-orphan in the Tulpa-zeroHP dismiss path, a chat-card reason-text conflation (Tulpa-at-0 said "the caster fell to 0 HP"), a stale JB2A asset key (`jb2a.impact.010.pink` doesn't exist in jb2a_patreon 0.6.91 — the strike already used `pinkpurple`), and a ghost-cast TypeError when `_updatePosition` ran against a torn-down element. v0.1.10 fixes all five. Smoke-test plan extended with rows R28–R32. No `_source/` changes — only `modules/*.js`, `lang/en.json`, and tests, so no pack rebuild required. **Note:** Bug 4 in the report (async `preUpdateActor` flag race) was a false positive — v0.1.9 already piggybacked the `relentlessUsed` flag onto the synchronous `changes` object via `foundry.utils.setProperty` ([modules/tulpa-hp-watcher.js:30-31](modules/tulpa-hp-watcher.js#L30-L31)); the smoke tester read the codebase against the v0.1.8 baseline. **Also note:** the `package.json` `version` field was stuck at `0.1.8` through v0.1.9; corrected to `0.1.10` here so `npm` and `module.json` agree.

### Fixed

- **`applyStrikeChanges` silently dropped every damage-type write (Bug 1, blocking).** v0.1.9's single-writer correctly funneled the damage-type override and every item-patch (`empoweredStrikes`) through one `strike.update()`, eliminating the v0.1.8 write-race. But the implementation copied each activity's `damage.parts` via `foundry.utils.deepClone(act.damage?.parts ?? [])`, and in dnd5e 5.2.5 `DamageData.types` is a `Set`. `deepClone` degrades `Set` instances into plain `{0: "bludgeoning"}` objects; `DamageData` then silently rejects the entire `system.activities.X.damage.parts = [...]` update because it expects an array or Set for `types`. Net effect: the Manifestation Strike kept its compendium-baked bludgeoning type regardless of dialog selection, and Empowered Strikes' Part-1 transformer ran but couldn't land either. Fixed by extracting a pure `buildStrikeParts(parts, damageType, itemPatches)` helper in [modules/modification-registry.js](modules/modification-registry.js) that calls `toTypesArray` (Set/array/object/null → plain array) and `freshenPart` (uses `toObject()` for DataModel instances, shallow spread for plain objects) to produce a brand-new parts array of plain objects — never round-tripping a `Set` through `deepClone`. [modules/cast-flow.js](modules/cast-flow.js)'s `applyStrikeChanges` now delegates to `buildStrikeParts`. The helper is pure (no Foundry globals) and unit-tested in [tests/modification-registry.test.mjs](tests/modification-registry.test.mjs) against Set / array / deepClone-degraded-object / null / undefined `types` shapes, with explicit assertions that only Part 0 gets the damage-type override (later parts stay untouched), Empowered Strikes still appends Part 1, the input parts array isn't mutated, and DataModel `toObject()` is honored — so a future dnd5e refactor of `DamageData` can't re-introduce the silent-update regression.
- **Token orphan when the Tulpa hit 0 HP (Bug 2, high).** Live-play in R16/R17 revealed that when the Tulpa dropped to 0 HP and the cascade fired, the anchor AE on the caster was correctly deleted but the Tulpa token sat on the canvas indefinitely. Root cause: in [modules/dismiss-flow.js](modules/dismiss-flow.js), the dismiss-animation call (`await playDismiss(...)`) was outside any error handler. When Sequencer threw on the missing `jb2a.impact.010.pink` asset (see Bug 5), execution unwound out of `onDeleteActiveEffect` *before* `tokenDoc.delete()` ran — so any future asset-resolution failure on a different damage type would have produced the same orphan. Fixed by wrapping `playDismiss` in a `try/catch` so animation failures only log a `console.warn` and the token-delete path always executes. Also replaced the v0.1.9 identity guard (`tokenDoc.parent?.tokens?.get?.(tokenDoc.id) === tokenDoc`) with a `.has(id)` membership check — when Foundry re-instantiates the document mid-delete, the identity check false-negatives and the delete is skipped, leaving the orphan. `.has()` is robust to that.
- **Dismiss chat card said "the caster fell to 0 HP" when the Tulpa dropped to 0 (Bug 3, medium).** Both the HP watcher (Tulpa at 0) and DAE's `specialDuration: ["zeroHP"]` on the anchor (caster at 0) wrote the same `dismissReason: "zeroHP"`, and `lang/en.json` mapped that single key to caster-focused text. Split into two reason codes: [modules/tulpa-hp-watcher.js](modules/tulpa-hp-watcher.js) now emits `"tulpaZeroHP"`, [modules/dismiss-flow.js](modules/dismiss-flow.js)'s `inferReason` returns `"casterZeroHP"` on the caster-HP-≤-0 branch, and [lang/en.json](lang/en.json) gains both keys: `tulpaZeroHP` → "the Tulpa was reduced to 0 hit points", `casterZeroHP` → "the caster fell to 0 HP". The old `zeroHP` key is removed (no callers remain). `chat-cards.js#postDismiss` falls through to the raw reason string when the i18n key is missing, so any legacy anchor that still carries `zeroHP` will read as the literal word rather than crashing.
- **Psychic dismiss animation referenced a nonexistent JB2A asset (Bug 5, medium).** [modules/animation-presets.js](modules/animation-presets.js)'s psychic preset listed `impact: { asset: "jb2a.impact.010.pink" }`, but jb2a_patreon 0.6.91 only ships `pinkpurple` for the `impact.010` family — the bare `pink` variant doesn't exist. Sequencer hung for 5s on every psychic dismiss/Relentless animation before the v0.1.6 timeout kicked in. The psychic *strike* asset already used `pinkpurple` (`jb2a.unarmed_strike.magical.pinkpurple`) so the two assets visually mismatched even before the dismiss broke. Changed `impact` to `jb2a.impact.010.pinkpurple` so the strike and impact share a colour suffix. Added two regression assertions in [tests/animation-presets.test.mjs](tests/animation-presets.test.mjs): a literal check for the psychic-impact path, and a parity check that every preset's `strike` and `impact` end with the same colour-suffix token — so a future partial palette update can't re-introduce a half-renamed asset chain.
- **`ManifestTulpaCastDialog._updatePosition` TypeError produced ghost casts (Bug 6, low).** ApplicationV2's default `_updatePosition` reads `this.element.offsetWidth` to size auto-height windows. When another module (BBMM, popout windows, tooltips) closed the dialog mid-positioning, `this.element` was null and the resulting `TypeError: Cannot read properties of null (reading 'offsetWidth')` aborted the render *after* `postUseActivity` already spent the spell slot — producing a "ghost cast": slot consumed, no dialog, no Tulpa, no cancel chat card. Fixed by overriding `_updatePosition` in [modules/cast-dialog.js](modules/cast-dialog.js) to bail out and return the position unchanged when `this.element` is null, then delegate to `super._updatePosition` otherwise so normal positioning still happens. Same fix protects every render path through the dialog.

### Internal

- **`buildStrikeParts` is now the single chokepoint for the strike's parts array** — the registry's `empoweredStrikes.patchActivity` already returned a parts array, and `applyStrikeChanges` no longer round-trips through `deepClone`, so the only place a `Set`-typed `types` field is ever read is `toTypesArray`. Future damage-type writes should compose through `buildStrikeParts` to inherit the Set-coercion guarantee.
- **Test suite expanded by 12 cases.** [tests/modification-registry.test.mjs](tests/modification-registry.test.mjs) adds 10 tests around `buildStrikeParts` (Set/array/deepClone-degraded-object/missing `types`, Part-0-only override, multi-part preservation, empty/undefined/null inputs, Empowered Strikes integration, DataModel `toObject()`, no-mutation). [tests/animation-presets.test.mjs](tests/animation-presets.test.mjs) adds 2 (psychic-impact literal, strike-impact colour parity).
- **`package.json` version is now in sync with `module.json`.** Both at `0.1.10`.

## [0.1.8] — 2026-05-26

> Addresses every actionable finding from the v0.1.7 smoke-test report ([docs/superpowers/test-plans/2026-05-25-v0.1.7-smoke-report.md](docs/superpowers/test-plans/2026-05-25-v0.1.7-smoke-report.md)). v0.1.7 was the first release where the cast flow surfaced runtime data-model bugs in dnd5e's `ActivityCollection` and Aura Effects' propagation contract — both core mechanics that silently no-op'd. v0.1.8 fixes the two blocking bugs (A, B), the cosmetic skill-label issue (C), and the two non-blocking dismissal papercuts (D, E). Smoke-test plan extended with rows R20–R24. No `_source/` changes — only `modules/*.js`, so no pack rebuild required.

### Fixed

- **Damage type selection and Empowered Strikes silently no-op'd (Bug A, blocking).** `setStrikeDamageType` in [modules/cast-flow.js](modules/cast-flow.js) and `empoweredStrikes.patch` in [modules/modification-registry.js](modules/modification-registry.js) both iterated `item.system.activities` with `Object.entries()`. In dnd5e 5.2.5, `activities` is an `ActivityCollection extends Map` — `Object.entries()` on a Map returns `[]` because Map entries aren't own enumerable string-keyed properties, so both loops never executed and the Manifestation Strike always dealt bludgeoning regardless of dialog selection. Fixed by extracting a shared `iterActivities(activities)` helper in [modules/modification-registry.js](modules/modification-registry.js) that uses the Map iterator protocol when `activities.entries` is a function and falls back to `Object.entries` for plain-object test fixtures; both call sites now go through the helper. Two regression tests in [tests/modification-registry.test.mjs](tests/modification-registry.test.mjs) lock the fix: one builds a real `Map` and asserts the +1d8 lands on every entry, the other exercises the helper directly across Map / plain-object / null / undefined inputs. Existing unit tests passed throughout v0.1.6 and v0.1.7 because they used plain-object fixtures where `Object.entries` works — the runtime-only divergence is now covered explicitly.
- **Harrowing Presence aura never propagated the marker to in-range hostiles (Bug B, high).** The v0.1.7 aura template populated `aura.changes` correctly (fixing the v0.1.6 bug) but Aura Effects 1.5.2 still produced no marker on nearby NPCs. Comparing against a working manually-authored aura preserved at repo root (`fvtt-ActiveEffect-harrowing-presence.json`, from the pre-module exploration phase) revealed that the `auraeffects.aura` data model has additional system fields with schema-defined defaults that nevertheless must be set explicitly for Aura Effects to wire up the propagation pipeline — most critically `collisionTypes: ["move"]`, without which Aura Effects never registers a proximity check on any token event. Fixed by adding the full schema block to `harrowingPresence.build` in [modules/modification-registry.js](modules/modification-registry.js): `collisionTypes: ["move"]`, `canStack: false`, `combatOnly: false`, `disableOnHidden: true`, `evaluatePreApply: false`, `overrideName: ""`, `bestFormula: ""`, `stashedChanges: []`, `stashedStatuses: []`. New unit test asserts each field is present in the built aura so a future refactor can't drop one back to the schema default and re-introduce the silent no-op.
- **Skill Affinity modifications displayed raw 3-letter codes (Bug C, cosmetic).** The dynamic skill-resistance loop in [modules/modification-registry.js](modules/modification-registry.js) wrote `name: ${skill}: ${skill.toUpperCase()}` — so the picker showed "Skill Affinity: STE" instead of "Skill Affinity: Stealth". `prettifySlug` couldn't help because `template.name` was already set. Fixed by replacing the array of 3-letter codes with an `Object.entries({ acr: "Acrobatics", ani: "Animal Handling", ... })` lookup that maps every dnd5e skill key to its full English name. Picker now reads as plain English; the comment notes that a future skill key added to `CONFIG.DND5E.skills` without a matching label entry falls back to the uppercased code so the dialog stays readable. Regression test scans all 18 skill modifications and asserts none of them still contain a 3-letter all-caps token in the display name.
- **`times-up` race produced a noisy console error on duration dismissal (Bug D, cosmetic).** When the 1-hour duration expired, both `times-up` and the dismiss-flow funnel raced to delete the Tulpa token. The second `tokenDoc.delete()` threw `EmbeddedCollection.get: undefined id [X] does not exist` — net result was correct (token gone, AE gone) but the error noise made every duration dismissal look broken. Fixed in [modules/dismiss-flow.js](modules/dismiss-flow.js) by checking the token is still present in its scene's collection (`tokenDoc.parent?.tokens?.get?.(tokenDoc.id) === tokenDoc`) before calling delete, and wrapping the delete in a try/catch as defense-in-depth for any other concurrent-delete scenario. Console is now clean on all five dismissal triggers.
- **Cast/dismiss flow stalled 5s on unindexed Sequencer assets (Bug E, cosmetic).** The 5s `withTimeout` wrapper (added in v0.1.6) kept the flow from hanging forever when jb2a wasn't installed, but every cast still paid the full 5-second timeout wait before the chat card posted. Added an `assetAvailable(asset)` pre-flight in [modules/animations.js](modules/animations.js) that consults `Sequencer.Database.entryExists` — when the database is loaded *and* explicitly reports the asset is missing, the play functions return immediately (no Sequence built, no timeout wait). When the database isn't ready or the function isn't available, we still attempt the play and fall back to the existing timeout safety net. `playManifest`, `playDismiss`, and `playRelentless` all share the same guard. Net effect: graceful degradation is now instant instead of taking 15s across cast + dismiss + Relentless.



> Addresses every actionable finding from the v0.1.6 smoke-test report ([docs/superpowers/test-plans/manifest-tulpa-test-report-v0.1.6.md](docs/superpowers/test-plans/manifest-tulpa-test-report-v0.1.6.md)). v0.1.6 was the first release where the cast flow completed end-to-end; the bugs exposed by that newly-reachable code path — broken weapon activities, dead aura propagation, non-functional initiative alignment, and a leaky dismissal funnel — are all fixed here. Smoke-test plan updated with rows R12–R19 to cover each fix.

### Fixed

- **Manifestation Strike shipped without Attack activities (Bug 5, critical).** The _source actor JSON has carried melee + ranged Attack activities since v0.1.0, but the v0.1.6 release zip was built from a stale LevelDB pack — so the spawned Tulpa's only weapon had `system.activities = {}`, with no roll button, no to-hit, no damage. `setStrikeDamageType` and `empoweredStrikes.patch` both iterated nothing and silently no-op'd. Root cause is pack-build hygiene: `npm run build:packs` was not re-run before tagging v0.1.6. Defense-in-depth fix lives in `scripts/validate-pack.js` — it now asserts every actor weapon item carries at least one `type: "attack"` activity with non-empty `damage.parts`. Two regression tests in `tests/validate-pack.test.mjs` cover both the missing-activities and empty-`damage.parts` failure modes, so a stale pack can't ship a fourth time.
- **Harrowing Presence aura propagated nothing to in-range hostiles (Bug 6, high).** The v0.1.6 implementation stored `inHarrowingAura` and `auraDC` on a separate `markerOnApply` AE that was never wired into Aura Effects 1.5.2's propagation contract. Aura Effects propagates the **aura AE's `changes` array** onto in-range targets — anything that lives elsewhere on the aura document is invisible to it. Rewrote `harrowingPresence.build` in `modules/modification-registry.js` to populate `aura.changes` with two OVERRIDE-mode changes (`flags.manifest-tulpa.inHarrowingAura = "true"` and `flags.manifest-tulpa.auraDC = String(dc)`), and dropped the `markerOnApply` field entirely. The `aura+marker` branch of `applyModifications` in `modules/cast-flow.js` simplified to a single AE creation. `modules/harrowing-presence-hook.js` now reads via `actor.getFlag("manifest-tulpa", "inHarrowingAura")` and coerces the DC with `Number(actor.getFlag(...))` since `getFlag` resolves against prepared post-AE data. Unit test rewritten to assert the sorted `changes` keys and their values.
- **Shared initiative never aligned the Tulpa (Bug 7, medium).** Two compounding causes: (a) when a player cast Manifest Tulpa mid-combat, the spawned Tulpa was not auto-added to the combat tracker, so `alignTulpaInitiative` had no combatant row to update; (b) `combatStart` fires *before* initiative is rolled, so even when the Tulpa was already in the tracker the alignment ran against `null` initiative values. Fix in `modules/initiative.js`: `alignTulpaInitiative` now calls `combat.createEmbeddedDocuments("Combatant", [{...}])` when the Tulpa isn't yet a combatant. New `onUpdateCombatant` export iterates `combat.combatants` to find Tulpas whose `summon.origin` matches the just-updated caster's actor UUID and re-aligns them to `caster initiative − 0.01`. Registered as `Hooks.on("updateCombatant", onUpdateCombatant)` in `modules/init.js`. This handles both the mid-combat-cast case and the re-roll-initiative case in a single funnel.
- **Re-cast dismissal chat card said "duration ended" (Bug 8, low).** `inferReason` in `modules/dismiss-flow.js` falls through to `"duration"` whenever the anchor AE deletion isn't otherwise tagged — and `times-up` is what cascades a re-cast's anchor deletion, so the times-up signal looked indistinguishable from a real duration expiry. Fixed by tagging the previous anchor with `flags["manifest-tulpa"].dismissReason = "recast"` in `cast-flow.js` immediately before deleting it. The cosmetic chat-card text now correctly identifies the reason.
- **Tulpa drop to 0 HP did not dismiss (Bug 9, high).** The v0.1.6 design relied on DAE `specialDuration: ["zeroHP", "isDeath"]` for HP-based dismissal — but those triggers operate on the *AE owner's* HP/status, not the referenced Tulpa actor. The Relentless watcher only existed for casts that included the Relentless mod, so any drop to 0 on a non-Relentless cast (or after Relentless was consumed) simply left the token sitting at 0 HP indefinitely. Replaced `modules/relentless-watcher.js` with `modules/tulpa-hp-watcher.js`, which arms on **every** cast in `cast-flow.js` via `armTulpaHpWatcher(tulpa.uuid, castConfig, damageType)`. The watcher's `preUpdateActor` interceptor: (a) if Relentless is present and unused → clamp HP to 1 + post the Relentless card + play the animation + set the `relentlessUsed` flag, (b) otherwise → tag the anchor with `dismissReason: "zeroHP"` and delete the anchor, cascading through the single-funnel dismissal. Re-armed on world load for any live anchor via `restoreTulpaHpWatchers()`.
- **Caster death cascade lost the Tulpa token (Bug 10, high).** When DAE's `specialDuration: isDeath` removed the anchor AE, `onDeleteActiveEffect` tried to resolve the Tulpa via `fromUuid(tulpaUuid)` — but the synthetic actor UUID (`Scene.X.Token.Y.Actor.Z`) can already resolve to null by then (the token document may be mid-teardown, or the prepared synthetic actor cache may be invalidated). Fix: the anchor AE now stores `tulpaTokenId` and `tulpaSceneId` flags alongside `tulpaUuid` and `castConfig` (created in `createAnchorAE`). `onDeleteActiveEffect` falls back to `game.scenes?.get(sceneId)?.tokens?.get(tokenId)` when the UUID resolution returns null, uses `tokenDoc.object` as the placeable for `playDismiss`, and `tokenDoc.delete()` for cleanup. Console.debug instrumentation added to trace the resolution path.

### Changed

- **Cast dialog labels are now human-readable (UX 1).** v0.1.6 surfaced raw registry slugs (`reinforcedForm`, `skill_ste`, `sizeShift_large`) in the modification picker. Added a module-scope `prettifySlug` helper in `modules/cast-dialog.js` that splits camelCase and snake_case into Title Case, and `_prepareContext` now attaches `displayName: m.template?.name ?? m.item?.name ?? prettifySlug(slug)` to each grouped entry. `templates/cast-dialog.hbs` renders `{{this.displayName}}` instead of `{{this.slug}}`.
- **Spell material text no longer double-wraps in parentheses (UX 3).** v0.1.6's material value was `"(a crystal shard imbued with your psychic resonance, worth at least 100 GP)"` — dnd5e's spell-card formatter then wrapped that in another set, displaying "((a crystal shard ...))". Stripped the outer parens from `_source/manifest-tulpa-spells/Item.manifest-tulpa.json` so the rendered card now reads with a single set, matching `manifest-tulpa.txt` RAW.

## [0.1.6] — 2026-05-25

> Addresses every actionable finding from the v0.1.5 smoke-test report ([docs/superpowers/test-plans/manifest-tulpa-test-report-v0.1.5.md](docs/superpowers/test-plans/manifest-tulpa-test-report-v0.1.5.md)). v0.1.5 was the first release where the cast dialog rendered in live Foundry; v0.1.6 fixes the run-time data-model and UUID-resolution bugs that were hidden behind the prior template crash. The cast flow now completes end-to-end in static analysis; downstream sections (3–8) remain unblocked but still need live Foundry verification.

### Fixed

- **`locateSummonedTulpa` could never find the freshly placed Tulpa token (cast-flow Bug 1).** The fallback path compared each canvas token's `flags.dnd5e.summon.origin` against `caster.uuid` with strict equality. `summon.origin` is an *item/activity* UUID (`Actor.<id>.Item.<id>[.Activity.<id>]`), so the comparison was always false and `abortAndCleanup` immediately ran against a `null` token — taking down every post-dialog step (stat adjustments, modifications, anchor AE, animations, chat card). Switched the comparison to a `startsWith` prefix match on `${caster.uuid}.`. Same actor-vs-item UUID confusion was also fixed in `initiative.js#onCombatStart` (new `casterUuidFromOrigin` regex helper that extracts the `Actor.<id>` prefix before `fromUuidSync`) and `dismiss-flow.js#onPreDeleteToken` (inline regex match on the origin string).
- **`setStrikeDamageType` wrote to the wrong damage path (cast-flow Bug 2).** The function targeted `system.damage.parts[0].types`, but in dnd5e 5.2.5 the Manifestation Strike's damage lives inside each *activity*'s `damage.parts` (`system.activities.<id>.damage.parts`). The old top-level path doesn't exist, so the `deepClone` returned an empty array, the length check bailed, and the damage type was never set — meaning the strike would have dealt untyped damage even if the rest of the flow had completed. Rewrote to iterate every activity on the strike and update the first damage entry of each, so both melee and ranged profiles carry the chosen damage type.
- **`empoweredStrikes.patch` crashed with `TypeError: Cannot read properties of undefined (reading 'push')` (modification-registry Bug 3).** Same root cause as Bug 2 in a different file — the patch cloned `strike.system.damage.parts` (undefined in 5.2.5) and then tried to `.push()` onto the result. Rewrote the patch to walk `system.activities.<id>.damage.parts` for every activity and append a `+1d8` of the chosen damage type, returning a multi-key update diff. Updated the corresponding unit test to assert the per-activity diff shape and to verify both the original and the added damage entries survive on every strike activity.
- **`playDismiss` (and `playManifest` / `playRelentless`) could hang indefinitely on missing Sequencer assets (animations Bug 4).** `.waitUntilFinished(-200)` resolves only when the visual finishes — if the asset is missing or stalled, the promise never resolves. The existing try/catch only caught thrown errors. Wrapped every Sequence `.play()` in a 5-second `Promise.race` timeout with a `.finally()`-cleared timer; the timeout rejection is then caught by the existing handler and downgraded to a single console warning, so the cast/dismiss/Relentless flow always continues even when the asset library isn't indexed.
- **`harrowing-presence-hook` could throw on the dnd5e 5.2.5 save API (test report §4 risk).** `actor.rollSavingThrow(...)` in dnd5e 5.2.5 may return `Array<D20Roll>`, a single roll, or `null`. The hook indexed `.total` directly on the return value, which would have NaN'd or thrown on the array path. Wrapped the call in a try/catch (warns and exits on failure) and normalized the return to the first roll's `total` before the DC comparison.

### Added

- **`applyCasterStats` step in the cast flow.** The compendium Tulpa actor ships with flat AC 13 / HP 40 / CR 1 / no spellcasting ability — dnd5e's summon `bonuses` and `match` fields aren't reliably honored by NPC statblocks in 5.2.5, so the v0.1.5 report observed the spawned Tulpa keeping all of those raw template values. Added an `applyCasterStats(tulpa, caster, slotLevel)` helper that runs immediately after `locateSummonedTulpa` returns and imperatively writes the spell formulas onto the spawned actor: AC `13 + spellMod`, HP `40 + 5 × casterLevel` (max + current), the caster's spellcasting ability + spell DC, a CR chosen to yield the caster's proficiency bonus (`profToCR` inverse mapping: prof 2→CR 1, 3→5, 4→9, 5→13, 6→17), and mirrored STR/CON save proficiencies. This keeps the source pack untouched (no `_key` regression risk, no compendium rebuild needed) and avoids tying the formulas to a specific summon-system field path.

## [0.1.5] — 2026-05-25

> Addresses the sole critical finding from the v0.1.4 smoke-test report ([docs/superpowers/test-plans/manifest-tulpa-test-report-v0.1.4.md](docs/superpowers/test-plans/manifest-tulpa-test-report-v0.1.4.md)). All downstream sections (3–8) that were BLOCKED in that report (modifications, Harrowing Presence, shared initiative, Relentless, dismissal, session reload) are now unblocked but still require live Foundry verification.

### Fixed

- **Cast dialog crashed on render with `Template part "body" must render a single HTML element`.** `templates/cast-dialog.hbs` rendered two top-level elements (`<section>` and `<footer>`), violating Foundry V13 `HandlebarsApplicationMixin`'s one-root-per-PART rule. Foundry parses the rendered HTML and rejects any part-template producing more than one top-level child, so the cast dialog never reached the DOM — taking the entire cast flow down with it (stat adjustments, modification application, anchor AE, Relentless arming, initiative alignment, manifest animation, chat card). The two-root-element problem was latent in v0.1.3 too, but the `{{in}}` helper crash there fired earlier in the pipeline and masked it; fixing the helper in v0.1.4 simply exposed the next layer. Fixed by wrapping the template's `<section>` and `<footer>` in a single `<div class="mt-cast-body">` root. No JavaScript changes — `_attachPartListeners` already scopes selectors to the part's root element.

## [0.1.4] — 2026-05-25

> Addresses every actionable finding from the v0.1.3 smoke-test report ([docs/superpowers/test-plans/manifest-tulpa-test-report-2026-05-25.md](docs/superpowers/test-plans/manifest-tulpa-test-report-2026-05-25.md)). The runtime tests that were BLOCKED in that report (Sections 3–8) are now unblocked but still require live Foundry verification.

### Fixed

- **Cast dialog crashed on render with `Missing helper: "in"`.** `templates/cast-dialog.hbs` used a Handlebars `{{in}}` helper that doesn't exist in Foundry V13's Handlebars environment, so the entire modification-selection dialog failed before paint — taking the whole cast flow (stat adjustments, modification application, anchor AE, Relentless arming, initiative alignment, manifest animation, chat card) down with it. Fixed by pre-computing `isSelected` per modification in `_prepareContext` and checking `{{#if this.isSelected}}` in the template. Also patched a latent secondary bug: `selected` was never exposed in the context, so the `../../selected` lookup would have been undefined even if the helper existed.
- **`module.json` `download` URL pinned to v0.1.0.** The in-repo manifest hard-coded `releases/download/v0.1.0/manifest-tulpa.zip`, so any user installing from the in-repo manifest (or any release where the CI rewrite step silently failed) would get the v0.1.0 zip regardless of advertised version. Switched the in-repo default to `releases/latest/download/manifest-tulpa.zip`. The release workflow's per-tag rewrite still produces version-specific URLs in published assets.
- **Duplicate hook registration possible across module re-imports.** Added a `globalThis.__manifestTulpaHooksRegistered` guard in `modules/init.js` that survives module re-import while the Hooks event bus is still alive — prevents `dnd5e.postUseActivity`, `dnd5e.combatTurnStart`, `combatStart`, `deleteActiveEffect`, and `preDeleteToken` from accumulating listeners in dev/reload workflows.

### Changed

- **Spell range and target now match the RAW.** `manifest-tulpa.txt` specifies "Range: 30 feet" but the packed spell had `range.units: "self"` at both the spell level and the summon-activity level, meaning the Tulpa could be placed anywhere on the canvas. Set both to `value: 30, units: "ft"` with `override: true` on the summon activity so placement is actually constrained. Target type is now `space` (was `self`).
- **Material component text matches the RAW.** `system.materials.value` now reads "(a crystal shard imbued with your psychic resonance, worth at least 100 GP)" to match `manifest-tulpa.txt`. Set `consumed: false` — the source text says "worth at least 100 GP" with no mention of consumption.

### Removed

- **Vestigial "Tether" feature on the Tulpa statblock.** A `Tether` feat ("If the Tulpa ends its turn more than 100 feet from its caster, it dissipates and the spell ends.") was present on the packed actor but is not in the spell text. Removed from `_source/manifest-tulpa-actors/Actor.tulpa.json` and from the `KEEP_ITEM_NAMES` allow-lists in both `scripts/scrub-source.mjs` and `scripts/validate-pack.js` so a re-scrub from the world export won't reintroduce it.

## [0.1.3] — 2026-05-25

> **Known issue (fixed in 0.1.4):** the modification-selection dialog crashed on render due to a missing Handlebars helper, taking the entire cast flow down with it. See the v0.1.4 entry for the full fix list.

### Fixed

- **Spell crashed dnd5e/midi-qol validation when dragged onto a character sheet.** v0.1.2 used document `_id`s `manifesttulpa0001` (17 chars) and `manifesttulpaspell` (18 chars). Foundry's pack loader was lenient enough to accept them, but `DocumentIdField` and `fromUuid` both require exactly 16 alphanumeric characters — so the moment the spell's summon-activity profile UUID was parsed, validation threw `Invalid document ID "manifesttulpa0001"`. Fixed by renaming the actor's id to `manifesttulpaA01` and the spell's id to `manifesttulpaS01` (both 16 chars). Validator now asserts every `_id` (top-level and embedded) matches `/^[a-zA-Z0-9]{16}$/`, with regression tests for actor / spell / embedded item.

## [0.1.2] — 2026-05-25

> **Known issue (fixed in 0.1.3):** the actor's `_id` was 17 characters and the spell's was 18 — Foundry's UUID validator rejects anything other than exactly 16 alphanumeric characters, so dragging the spell onto a character sheet threw `Invalid document ID`.

### Fixed

- **Compendium packs were empty.** v0.1.0 and v0.1.1 both shipped with empty LevelDB packs — the spell and statblock did not appear in Foundry. `scripts/scrub-source.mjs` set each doc's `_id` but not its `_key`, and `@foundryvtt/foundryvtt-cli`'s `compileClassicLevel` silently skips any document missing `_key`. Fixed by writing the full `_key` chain (`!actors!<id>`, `!actors.items!<actorId>.<itemId>`, etc.) into the scrubbed source, and added validator assertions + regression tests so this can't ship a third time.

## [0.1.1] — 2026-05-25

> **Known issue (fixed in 0.1.2):** packs shipped empty because the scrub script omitted the `_key` field required by foundryvtt-cli.

### Fixed

- **`module.json` required/recommended IDs.** v0.1.0 declared `aura-effects` and `automated-animations` — those are the human-readable names, not the Foundry module IDs. Foundry flagged both as missing on install even when the correct modules were present. Corrected to `auraeffects` (mclemente's "Aura Effects") and `autoanimations` (tposney's "Automated Animations").

## [0.1.0] — 2026-05-25

Initial public release. Ships the full automation of the 5th-level Conjuration spell *Manifest Tulpa* for FoundryVTT V13 + dnd5e 5.2.5.

> **Known issue (fixed in 0.1.1):** declared the wrong Foundry IDs for Aura Effects and Automated Animations, causing Foundry to flag them as missing dependencies even when installed.

### Added

- **Spell + summoned actor compendia.** Two LevelDB packs — `manifest-tulpa-spells` (the spell item with a `summon` activity) and `manifest-tulpa-actors` (the base Tulpa statblock). Source JSON lives in `_source/`; packs are built at release time.
- **Cast-time dialog.** Player picks one damage type (force / radiant / psychic) and 0-N modifications grouped by category (Form, Combat, Movement, Skill, Utility). Slot-based budget (2 at spell-slot 5, +1 per upcast, capped at 6).
- **Modification registry.** 15 modifications across item-patch, item-insert, AE-only, and aura-with-marker kinds:
  - Form: Reinforced Form, Unsettling Form, Vital Surge, Size Shift (Tiny / Small / Large / Huge — mutually exclusive)
  - Combat: Empowered Strikes, Multiattack, Harrowing Presence, Relentless
  - Movement: Fly Speed
  - Skill: Skill Affinity (Stealth / Perception / Insight / Athletics)
  - Utility: Resistance, Telepathic Link
- **Cast flow (`dnd5e.postUseActivity`).** Runs *after* dnd5e's native slot dialog so the slot level is known. Sets strike damage type, applies modifications, creates the caster-side anchor AE, arms the Relentless watcher, aligns initiative, plays the manifest animation, and posts the cast chat card. Orphan tokens are cleaned up if the player cancels or exceeds the slot budget.
- **Single-funnel dismissal.** A caster-side AE flagged with `flags["manifest-tulpa"].tulpaUuid` is the source-of-truth for an active Tulpa. All five dismissal triggers flow through `deleteActiveEffect`:
  1. Duration expiry (1 hour, via `times-up`)
  2. Tulpa drops to 0 HP (DAE `specialDuration: zeroHP`)
  3. Caster dies (DAE `specialDuration: isDeath`)
  4. Caster re-casts (previous anchor deleted at cast-start)
  5. GM manually deletes the Tulpa token (`preDeleteToken` hook)
- **Harrowing Presence aura.** Two-stage: an Aura Effects aura on the Tulpa applies a marker AE (carrying `inHarrowingAura: true` + `auraDC`) to in-range hostiles; a global `dnd5e.combatTurnStart` hook reads the marker, rolls Wis save vs. caster's spell DC, and applies `frightened` on failure (auto-clears next turn via `times-up`).
- **Relentless watcher.** `preUpdateActor` intercept clamps HP to 1 the first time the Tulpa would drop to 0, posts the Relentless chat card, plays the visual, and disarms itself. Watchers are restored on world load for any live anchor.
- **Shared initiative.** When cast during active combat, the Tulpa enters the tracker at the caster's initiative − 0.01 (directly after).
- **Animations.** Sequencer-backed manifest / dismiss / Relentless effects with damage-type tinting (force=blue, radiant=gold, psychic=magenta). Gracefully degrade with a single console warning when `jb2a_patreon` is absent — mechanics still work.
- **Chat cards.** Localized cast, dismiss, Relentless, telepathic-link, and warning cards in `lang/en.json`.
- **Build & release tooling.**
  - `scripts/scrub-source.mjs` — strips world-export flags from `_source/` JSON.
  - `scripts/validate-pack.js` — pre-release assertions (no leftover modification AEs, summon UUID points at packed actor, no scaling, etc.). Includes positive + injection unit tests.
  - `scripts/build-packs.mjs` — wraps `@foundryvtt/foundryvtt-cli` to produce LevelDB packs.
  - `.github/workflows/release.yml` — tag-driven release (`v*`): rewrites `module.json` version + download URL, validates, tests, builds packs, zips the module, and publishes via `softprops/action-gh-release@v2`.
- **Manual smoke-test plan.** [docs/superpowers/test-plans/2026-05-24-manifest-tulpa-smoke.md](docs/superpowers/test-plans/2026-05-24-manifest-tulpa-smoke.md) covers every modification, all 5 dismissal triggers, the aura, Relentless, shared initiative, session reload, and required-module failure modes.

### Required modules

- `dnd5e` ≥ 5.2.5
- `midi-qol`
- `dae`
- `times-up`
- `sequencer`
- `portal-lib` (theripper93's "Portal" — **not** `portal`)
- `auraeffects` ≥ 1.5.2 ("Aura Effects" by mclemente)

### Recommended modules

- `jb2a_patreon` — for full manifest / dismiss / Relentless visuals
- `autoanimations` — for strike animations ("Automated Animations" by tposney)

### Known limitations

- The exact applied-effect schema slot used by Aura Effects 1.5.2 (`system.appliedEffect` vs. another key) could not be verified outside a real Foundry runtime. Marker propagation should be verified in-world per smoke-test step *Harrowing Presence #3*; if the marker flags are missing on the propagated NPC AE, re-wire the `aura+marker` branch in [modules/cast-flow.js](modules/cast-flow.js).

### Install

End-user manifest URL:

```
https://github.com/themrbeasley/manifest-tulpa/releases/latest/download/module.json
```

[0.1.11]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.11
[0.1.10]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.10
[0.1.8]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.8
[0.1.7]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.7
[0.1.6]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.6
[0.1.5]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.5
[0.1.4]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.4
[0.1.3]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.3
[0.1.2]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.2
[0.1.1]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.1
[0.1.0]: https://github.com/themrbeasley/manifest-tulpa/releases/tag/v0.1.0
